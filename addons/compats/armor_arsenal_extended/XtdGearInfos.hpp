class XtdGearInfos
{
    class CfgWeapons
    {
        class CLASS(Helmet_Airborne_Base)
        {
            model = QCLASS(Helmets_Airborne);
            camo = "Base";
        };

        class CLASS(Helmet_Airborne_CT)
        {
            model = QCLASS(Helmets_Airborne);
            camo = "CT";
        };

        class CLASS(Helmet_Airborne_CT_v2)
        {
            model = QCLASS(Helmets_Airborne);
            camo = "CT_v2";
        };

        class CLASS(Helmet_ARC_Base)
        {
            model = QCLASS(Helmets_ARC);
            camo = "Base";
        };

        class CLASS(Helmet_ARC_CT)
        {
            model = QCLASS(Helmets_ARC);
            camo = "CT";
        };

        class CLASS(Helmet_ARC_CT_Legacy)
        {
            model = QCLASS(Helmets_ARC);
            camo = "CT_Legacy";
        };

        class CLASS(Helmet_ARC_NightShade)
        {
            model = QCLASS(Helmets_ARC);
            camo = "NightShade";
        };

        class CLASS(Helmet_ARC_Enlisted_CamoBrown)
        {
            model = QCLASS(Helmets_ARC);
            camo = "Enlisted_CamoBrown";
        };

        class CLASS(Helmet_ARC_Enlisted_CamoGrey)
        {
            model = QCLASS(Helmets_ARC);
            camo = "Enlisted_CamoGrey";
        };

        class CLASS(Helmet_ARC_Enlisted_CamoNight)
        {
            model = QCLASS(Helmets_ARC);
            camo = "Enlisted_CamoNight";
        };
    };
};